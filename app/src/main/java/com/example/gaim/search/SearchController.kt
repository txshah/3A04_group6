package com.example.gaim.search

// (this is lowkey the HOME PAGE backend controller
// receives which searches to run from HOME PAGE and then runs each search
//after receiving results, it passes results to FORUM
//once forum sends back RESULT, sends info to HOME PAGE
//
// if HOME PAGE requests report, passes RESULT (received from HOME PAGE) to REPORT GENERATOR
// and returns result to home page
class SearchController {
//    handle if user selects accoutn - view old
//    handle if users starts search
//    get front end data and send to start search (if thats what is requested by user in front end)
//    sends results back and if user clicks save - send to there

}